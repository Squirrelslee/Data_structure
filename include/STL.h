/*
这里对C++各种容器测试
以及各种容器嵌套等，涉及一些注意事项，比如调用STL中某些功能，返回bool还是iterator还是non-oid
构造函数参数传递，如何使用new将数据开辟堆区，调用的构造函数是什么
对于尚未使用过的方式进行记录
*/
#pragma once
#include <iostream>
#include <vector>
#include <list>
#include <algorithm>
#include <map>
#include <unordered_map>
#include <stack>
#include <queue>

